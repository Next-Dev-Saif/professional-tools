import { SOPWriterClient } from '@/components/page-sections/sop-writer/SOPWriterClient';

export const metadata = {
  title: 'SOP Writer | Hephaestus Tools',
  description: 'Standard Operating Procedures',
};

export default function SOPWriterClientPage() {
  return <SOPWriterClient />;
}
