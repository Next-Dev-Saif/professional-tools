import { SLAGeneratorClient } from '@/components/page-sections/sla-generator/SLAGeneratorClient';

export const metadata = {
  title: 'SLA Generator | Hephaestus Tools',
  description: 'Service Level Agreement',
};

export default function SLAGeneratorClientPage() {
  return <SLAGeneratorClient />;
}
