import { PRDBuilderClient } from '@/components/page-sections/prd-builder/PRDBuilderClient';

export const metadata = {
  title: 'PRD Builder | Hephaestus Tools',
  description: 'Product Requirements Document',
};

export default function PRDBuilderClientPage() {
  return <PRDBuilderClient />;
}
